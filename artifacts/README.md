# Artifacts

Purpose:
- Hold generated outputs from deterministic assessments and AI-generated summaries.

Working artifact areas:
- `assessments/` for per-asset machine-readable outputs
- `briefs/` for human-readable upgrade briefs
- `portfolio/` for roll-up summaries and reporting outputs
