# Prototype Input

Purpose:
- Hold file-based inputs used to prototype inventory ingestion, lifecycle policy loading, and assessment generation before system integrations exist.

Working input areas:
- `inventory/` for normalized or raw asset snapshots
- `lifecycle/` for lifecycle policy source extracts or curated policy records
- `assessments/` for deterministic rule inputs and expected outputs used in test scenarios
